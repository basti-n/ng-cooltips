import { NgCooltipsDirective } from './ng-toolstips.directive';

describe('NgCoolstipsDirective', () => {
  xit('should create an instance', () => {
    const directive = new NgCooltipsDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
